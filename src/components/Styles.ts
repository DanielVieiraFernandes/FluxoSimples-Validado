import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    btt:{
        backgroundColor: '#000',
        color: '#fff',
        padding: 20,
        borderRadius: 8,
    },
    txtInput: {
        width: 300,
        height: 50,
        borderColor: '#000',
        borderWidth: 1,
        borderRadius: 10,
        padding: 10
    }
})